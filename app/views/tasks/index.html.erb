<h1>To Do:</h1>

<ul>
  <%@tasks.each do |task| %>
    <li>
      <h2><%= link_to task.title, task_path(task) %></h2>
      <p>Completed: <%= task.completed? %></p>
    </li>
  <% end %>
</ul>

<%= link_to 'New Task', new_task_path %>
